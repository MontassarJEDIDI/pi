import { MedicalTreatmentService } from './medical-treatment.service';

describe('MedicalTreatmentService', () => {
  it('should be created', () => {
    const service = new MedicalTreatmentService();
    expect(service).toBeTruthy();
  });
});

